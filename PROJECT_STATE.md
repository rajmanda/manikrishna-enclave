# PROJECT_STATE.md

Last updated: 2026-07-23

| Field | Value |
|---|---|
| Current version | 0.12.0 |
| Current milestone | **M4 Governance — complete. PRD Phases 1–4 fully shipped.** Next: M5 (AI, mobile) or hardening |
| Current sprint | Launch readiness (real owner emails, notifications decision) |
| Current branch | feature/community-switching (off main post-PR#3) |
| Last completed feature | Design-system + UI overhaul (tokens, primitives, Framer Motion, `/home` landing, grouped nav, page transitions — see `UX_REVIEW.md`) + per-deployment branding ("Manikrishna Enclave") |
| Current feature | **Delivery-agent health banner + stale-queue sweep (2026-07-23, working tree)**: OpenClaw polls now stamp a heartbeat (`agent_status`); new manager-only `GET /notification-queue/health`; amber "WhatsApp delivery is paused" banner in AppShell (all pages, managers only, re-checks every 60s) when the agent is silent > 5 min with messages queued; health/delivery-summary endpoints lazily sweep stale entries (pending > 2h, processing > 1h → failed, community-scoped, audited) so stuck messages surface as resendable badges. 4 new tests (251 total), build clean — **awaiting Raj's local test + confirmation before commit/push**. Previous: **Notification links → community.nivaasos.com + emoji prefixes (2026-07-23, working tree)**: all 17 outbound message templates hardcoded the retired community.rajmanda.com — new `app_base_url` setting (default https://community.nivaasos.com) + `app_url()` helper in `notification_service.py`, every enqueue site now uses it; CORS default likewise swapped; invoice/payment messages gained emoji prefixes (🧾 invoices/assessments, ✅ confirmations, ⚠️ rejections, 💳 credits) to match 📢/🛠️/🔧/💰. 247 tests pass — **awaiting Raj's confirmation before commit/push**. Previous: **Failed-notification badges + resend (2026-07-22, working tree)**: terminally failed `notification_queue` deliveries now badge the source entity inline for managers/admins ("Not delivered" + Resend via existing retry endpoint) across work orders (list+detail), maintenance, expenses, feed, invoices (rows + one strip for batch/credit failures), and cost-case detail. Backend: `related_type`/`related_id` on `NotificationRecord` populated at every enqueue site, `GET /notification-queue/delivery-summary` (grouped by entity, WRITE_ROLES only), list-endpoint filters, new compound index, migration 009 payload backfill (schema v9). Frontend: shared `DeliveryStatus.tsx` (hook + badge + panel); residents/auditors make no delivery requests and see nothing. 7 new tests (247 total), frontend build clean — **awaiting Raj's local test + confirmation before commit/push**. Previous: **Community WhatsApp events → group chat (2026-07-21, working tree)**: work-order created/stage/owner-approval notifications now enqueue ONE `recipient_phone="group"` entry (OpenClaw resolves to the group JID) instead of fanning out per member; new `enqueue_for_community_group` helper; feed + expense group sends refactored onto it; community-visible maintenance requests now announce in the group (`maintenance_request_created` event type; private requests stay silent); per-apartment money messages stay individual. 240 tests pass — **awaiting Raj's local test + confirmation before commit/push**. Separate infra issue found: ALL 25 WhatsApp sends for Vishnu's "Sewer line…" work order failed on the Mac mini poller with `[Errno 13] Permission denied: 'openclaw'` (retries exhausted) — the openclaw binary the poller invokes isn't executable; fix on the Mac mini. Previous: **Marketing CTAs → Growth Center CRM — SHIPPED (merged PR #23, deployed 2026-07-22)** + same-day follow-ups: mailto flow REMOVED entirely from `LeadForm` (owner decision — never open the visitor's mail app; failure = retry message with contact address as text), Terraform `CORS_ORIGINS` gained nivaasos.com/www origins (applied, preflight verified), marketing image redeployed via deploy.yml `deploy_marketing=true`, and `MARKETING_APP_URL` repo variable flipped to https://community.nivaasos.com so Resident Login targets the nivaasos app domain (cert ACTIVE; Google sign-in there still needs the OAuth client origins — owner action). Original feature: every nivaasos.com CTA form (demo/start/waitlist/contact) now POSTs to new public endpoint `POST /api/v1/public/leads` (`backend/app/routers/public_leads.py`, no auth) which creates a `growth_leads` CRM entry (source `website`, tags `["website", <kind>]`, stage `new`, next-action for the follow-up tracker) + lead activity + growth audit + WhatsApp heads-up via OpenClaw queue; honeypot + per-IP (5/h)/global (200/h) rate limits + length caps; `LeadForm` keeps mailto as automatic fallback (`NEXT_PUBLIC_LEADS_API_URL`, empty = mailto-only) and updated privacy copy. 6 new tests (240 total), marketing build clean, exercised end-to-end against Atlas dev (lead+activity+queue verified, then cleaned up). **Deploy prerequisite: add `https://nivaasos.com,https://www.nivaasos.com` to the deployed backend's `CORS_ORIGINS`** (env overrides the new code default). **Awaiting Raj's confirmation before commit/push.** Previous: **Resident Login popup + mobile modal fix (2026-07-18)**: marketing-site "Resident Login" (header/hero/final CTA) now opens a popup (`marketing/src/components/ResidentLogin.tsx`) that renders Google sign-in on nivaasos.com when `NEXT_PUBLIC_GOOGLE_CLIENT_ID` is set and hands the credential to the app via `#gcred=` fragment (app login page consumes+scrubs it, exchanges via existing `/auth/google` → dashboard; graceful link fallback when the ID is unset). Owner actions: add https://nivaasos.com to OAuth authorized JS origins + set the env in the marketing build. Also: shared app `Modal` is now a centered dialog with margins on every viewport (was a flush mobile bottom sheet that forced scrolling; new `.modal-panel-max` = 85dvh cap so panels clear mobile toolbars) — fixes /community tile popups and every Modal-based dialog. Both builds clean, 234 backend tests pass — **awaiting Raj's local test + confirmation before commit/push**. Previous: **Nivaasos public marketing site (2026-07-18, working tree)**: new isolated `marketing/` Next.js app for nivaasos.com — 24 fully static routes (landing + 18 intent pages), per-page SEO metadata + JSON-LD, robots/sitemap/llms.txt, fictional Greenwood Residency demo data only, mailto lead forms (no unapproved endpoint), Dockerfile ready; deployment (LB host rule + DNS) documented but NOT executed — see docs/NIVAASOS_PUBLIC_SITE.md; build clean, **awaiting Raj's confirmation before commit/push**. Owner pre-launch decisions: hello@nivaasos.com mailbox, training-crawler policy, privacy/terms review. Previous: **Growth Center (2026-07-18, feature/growth-center)**: isolated Super Admin marketing workspace at `/super-admin/growth-center` — own API namespace `/api/super-admin/growth-center`, own MongoDB database via `GROWTH_CENTER_MONGO_URI` (unset ⇒ 503, never falls back to the operational DB), `growth_` collections, module-local audit trail. Seeds a default Hyderabad property-manager acquisition playbook (lead magnets, qualification, 5-step outreach sequences, 10 objection scripts, pilot positioning, 3 personas) with Draft/Review/Approved/Archived lifecycle, duplicate/restore/copy/export/search — all scoped to module data only. Only approved reuse: `app.core.security` (auth/role) + visual components; static import-boundary test enforces it. PLUS Leads CRM (same day): `/super-admin/growth-center/crm` — 9-stage pipeline with follow-up tracker and activity timeline, manual + Firecrawl web discovery of Hyderabad property-manager prospects (`FIRECRAWL_API_KEY`, human-reviewed import, dedupe; social networks never scraped). 23 new tests (233 total), build clean — **awaiting Raj's confirmation before commit/push**. See `docs/GROWTH_CENTER.md`. Previous: Third-party payments (2026-07-17, feature/tenant-payment): tenant pays the OWNER's HOA invoice on the owner's behalf — payer attribution on payments/credits (payerType/Name/EntityId, collectedBy, deposit status, notes), invoice responsibility vs payment-request routing (owner always liable; request routable to the active tenant, per-apartment select in Generate + PATCH re-route), receipt PDFs addressed to the actual payer (`GET /payments/{id}/receipt.pdf`), overpayment→payer-funded credit, `POST /payments/advance` (money before its invoice) + credit refunds to the funder, void-and-replace for posted payments (UI no longer hard-deletes), migration 008 backfill + reclassification report. PLUS owner-side payer declaration: "I've paid this"/Pay-multiple gain a "Who paid?" select (self / my tenant / someone else), claimed payer shown on the pending card, confirming manager stamped as collectedBy. 209 tests, build clean, merged via PR #13. Previous: Whitelist accountId fix (2026-07-17, on main): `POST /users` now persists `account_id` — members created via the API/bulk-whitelist lost their multi-apartment Account link (hit in prod: msvram@gmail.com lost 401+402 view); regression test added; data repair: re-link via the member dialog's Account dropdown. Previous: Invoices-page cleanup (2026-07-17, on main): REMOVED the manager-side Combined Payment (button, `CombinedPaymentDialog`, `POST /payments/allocate`) — owner decision, it allowed cross-apartment allocation with excess credit landing on an arbitrary apartment; owner-side `/payments/report-batch` remains the supported multi-invoice flow. Also: "Create invoices" button renamed to "Create Community Invoices" and Service fees moved next to it (before Bill owner). 193 tests, build clean. Previous: Pay multiple invoices + advance credits (merged PR #12): owner ticks invoices on their cards → sticky pay bar → dialog → `POST /payments/report-batch` (one pending per-invoice payment sharing a batchId — balancing identical to individual); batches are ALL-OR-NONE (manager confirms/rejects the whole claim, reasons relayed + durable `payment_rejections` on the invoice card); confirm never overshoots an invoice (excess banked as credit) and apply-credit skips claimed invoices (money conserved); a correction-request flow was prototyped and REMOVED pending better design; overpayment banked in `credits` (pending until batch confirmed), spent FIFO via `POST /payments/apply-credit` (owner tile + per-invoice "Pay from credit" + manager credits card); dev refresh-db now drops dev-only collections (stale-credits fix); PLUS two small follow-ups (2026-07-16): invoices UI hides the delete button for property managers on paid-off invoices (mirrors the committed backend 403) and the Add/Edit Member dialog accepts comma-delimited emails to bulk-whitelist (shared role/apartment, names derived from each email, duplicates skipped + reported); 194 tests, build clean, smoke-tested on Atlas dev — **awaiting Raj's confirmation before commit/push**. (Parked on feature/tenant: tenant lite experience + direct messages.) Previous: Money-chain integrity (bore well fix): Jun anchor amended +2,700 (prod+dev, audited), /reserve-fund/reconciliation endpoint + Reserve page warnings (unanchored activity, collections without expense), closed-month notes in payment/expense dialogs, chain links MR→WO→expense/invoices with WO Money panel + prefilled creation flows (155 tests) — awaiting confirmation before push. Merged (PR #7): Expense-entry UX fix (manager complaint): dedicated /expenses page + Money-nav entry, Dashboard quick-actions row with dialog deep-links, receipt picker inside Add Expense dialog, form polish. Previously merged (PR #6): Platform Insights — super-admin CEO dashboard at `/insights` (adoption funnel, daily engagement from audit trail, module usage, financial pulse, per-community health, whitelist-adoption chase list; `GET /insights/platform` + `users.last_login`/`login_count` stamping, 149 tests). Also merged in PR #6 — Invoice receipts: upload/photograph paper receipts from all four invoice dialogs and the invoice sheet (past invoices); saved to Documents with apartment-scoped visibility (`POST /invoices/{id}/receipt`, `documents.apartment_ids`/`invoice_id`, visibility-filtered list/download), 4 new tests (145 total). Previous: Setup Assistant (guided 3-step community onboarding, /setup + setup router, household naming) + community switching (super admin acts inside any owned community) + multi-community memberships (same email across communities: per-community whitelist uniqueness, switch-membership endpoint, AppShell community dropdown). Previous: multi-community portfolio console (super-admin): `GET /communities/portfolio/stats` + `/portfolio` page, 4 new tests (112 total). Presentation page rebuilt as role-based decks (6 audiences) with persistent CTAs, WhatsApp-alerts slide, privacy slide, Early Access badges on portfolio claims |
| Next priority | Owner onboarding: collect real owner emails and whitelist them; then notifications provider decision |
| Deployment status | **Live at https://community.rajmanda.com** (Cloud Run, asia-south1) · **Marketing site live at https://nivaasos.com** (same LB, `nivaasos-marketing` service; deployed via deploy.yml `deploy_marketing` job 2026-07-22 with the CTA→CRM lead form) |
| Database version | Schema v9 (migrations 001-009; 009 = notification related-ref backfill, in working tree); Atlas `hyderabad.n5kr48f` (AWS Mumbai), DB `communityhub` (prod, live since 2026-07-12 IST) / `communityhub_dev` (local); legacy `manikrishna_enclave` in ~1-week bake before drop |
| Infrastructure version | Terraform applied — 41 resources (nivaasos.com marketing service/cert/host-rule + `nivaasos-community-cert` and community.nivaasos.com on the app host rule for the domain transition; both applied 2026-07-19, community.rajmanda.com unaffected; community cert pending owner DNS; backend `CORS_ORIGINS` gained nivaasos.com + www origins for the public lead endpoint, applied 2026-07-22), state in gs://mm-owners-5b8611-tfstate |
| Last deployment | 2026-07-22 via deploy.yml (manual dispatch, `deploy_marketing=true`) |

## Live URLs

- Frontend: https://communityhub-frontend-ht4p2vwsjq-el.a.run.app
- API: https://communityhub-backend-ht4p2vwsjq-el.a.run.app (`/health`, `/docs`)
- https://community.rajmanda.com — live (LB 34.120.210.248, cert ACTIVE)

## Owner action items (blockers for M1 close-out)

1. ~~DNS~~ ✅ done 2026-07-04 — cert ACTIVE, https://community.rajmanda.com live.
2. ~~Atlas Network Access~~ ✅ already allowed (cluster shared with other Cloud
   Run apps); verified via clean API startup logs.
3. ~~Google OAuth client~~ ✅ done 2026-07-04 — client ID in Secret Manager
   (v2), GitHub variable, and local envs; Google sign-in live.
4. **Recommended:** rotate the Atlas password (it was shared in a chat
   session) and update the `communityhub-mongodb-uri` secret + local .env.
5. Replace placeholder owner emails with real Google accounts
   (`PATCH /api/v1/users/{id}`).

## Completed so far

- Phase 1 app: mobile-first frontend (17 routes) + FastAPI backend (OAuth
  whitelist, RBAC, tenant isolation, audit log, dashboards, read APIs),
  frontend↔API integration, 28 tests. Atlas e2e verified locally.
- M1: git repo + GitHub (public), CI (backend tests + frontend build, green),
  Terraform (Cloud Run ×2, Artifact Registry, communityhub-* secrets, WIF,
  global HTTPS LB with same-origin /api/* routing), deploy workflow via WIF,
  first production deploy with health checks, full docs/ set.

## Seed data status

None — `frontend/src/lib/data.ts` deleted in M4; every screen is API-driven.

## Known issues

1. ~~Dev and prod share DB~~ ✅ 2026-07-11: data ported to generic DBs and
   **prod cutover completed 2026-07-12 07:59 IST** (terraform apply; backend
   revision 00041 serving `communityhub`, health 200, OpenClaw poller
   confirmed live; jobs backup/cleanup updated too). Local dev on
   `communityhub_dev`. Old `manikrishna_enclave` DB retained read-only for a
   ~1-week bake (check its audit_log stays frozen), then dump + drop.
   Pre-cutover JSON backup: backend/pre_cutover_manikrishna_enclave_2026-07-11.json.
2. Atlas invoice `inv-2606-502` shows ₹5,500 vs seed's ₹3,500 — DB predates/
   was edited outside the seed; treat Atlas as source of truth.
3. No staging environment yet (deliberate — add when M2 warrants).
4. Terraform runs locally, not in CI (deliberate for now).
5. No rate limiting on /auth/*; JWT in localStorage (D-006) — M2 window.
6. In-app notifications static; no pagination on list endpoints.

## Open decisions

- Payment gateway for M2 (or record-only first).
- Staging topology when needed.
